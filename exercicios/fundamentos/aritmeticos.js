const [a, b, c, d] = [3, 5, 1, 15] // atribuindo com destructuring

const soma = a + b + c + d //adição
const subtracao = d - b //  subtração
const multiplicacao = a * b // multiplicação
const divisao = d / a // divisão
const modulo = a % 2 // modulo - resto da divisão

console.log(soma, subtracao, multiplicacao, divisao, modulo)